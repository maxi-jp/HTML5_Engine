# Debugging Tools

To aid in development, the engine provides built-in debugging features:

- **Physics Debug Drawing** — when using Box2D, call `SetDebugDraw(true)` on your `Box2DGame` to visualise physics bodies, joints, and collision shapes overlaid on the canvas.
- **FPS / Stats Overlay** — an optional overlay can display frames per second and other performance statistics.
